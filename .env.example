# Copy this file to .env and fill in real values. .env is git-ignored. NEVER commit real keys.
TAPFIX_DATA_DIR=./data
TAPFIX_LOG_LEVEL=INFO

# Used from Step 4 (LLM layer). Leave empty for now.
LLM_PROVIDER=
LLM_MODEL=
LLM_API_KEY=
