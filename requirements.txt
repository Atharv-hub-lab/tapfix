# Step 1 foundation. Later steps add: rank_bm25, sentence-transformers, numpy, an LLM client.
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29
pydantic>=2.6,<3.0
python-dotenv>=1.0
pytest>=8.0
httpx>=0.27          # needed by FastAPI's TestClient
